#include <iostream>
#include <cmath> //declares functions to compute common mathamatical operations
#include "RandomNumberGenerator.h"


using namespace std;

// all of these goign down are prototypes allowing you just to use the line instead of writting down all the code.


int CreateRandomNumber(int minimumValue,
					   int maximumValue );

double CreateRandomNumber(double minimumValue,
					   double maximumValue );
